import React, { Component } from "react";
import Option from "./Option";

const Select = () => {
  return (
    <select>
      <Option />
      <Option />
      <Option />
    </select>
  );
};

export default Select;
